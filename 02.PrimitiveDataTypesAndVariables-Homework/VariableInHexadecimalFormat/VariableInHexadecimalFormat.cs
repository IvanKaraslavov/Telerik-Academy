using System;

        //Declare an integer variable and assign it with the value 254 in hexadecimal format (0x##).

class VariableInHexadecimalFormat
{
    static void Main()
    {
        int hexademical = 0xFE;
        Console.WriteLine(hexademical);
    }
}
